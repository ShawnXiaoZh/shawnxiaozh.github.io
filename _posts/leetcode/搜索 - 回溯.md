<font style="color:rgb(78, 83, 88);">回溯算法是一种通过不断 尝试 ，搜索一个问题的一个解或者 所有解 的方法。在求解的过程中，如果继续求解不能得到题目要求的结果，就需要 回退 到上一步尝试新的求解路径。回溯算法的核心思想是：在一棵 隐式的树（看不见的树） 上进行一次深度优先遍历。</font>

## [78. 子集](https://leetcode.cn/problems/subsets/)
```java
class Solution {
  public List<List<Integer>> subsets(int[] nums) {
    List<List<Integer>> res = new ArrayList<>();
    dfs(nums, 0, new ArrayList<>(), res);
    return res;
  }
  private void dfs(int[] nums, int i, List<Integer> list, List<List<Integer>> res) {
    if(i == nums.length) {
      res.add(new ArrayList<>(list));
      return;
    }
    // choose
    list.add(nums[i]);
    dfs(nums, i + 1, list, res);
    list.remove(list.size() - 1);
    // not choose
    dfs(nums, i + 1, list, res);
  }
}
```





```java
class Solution {
  public List<List<Integer>> subsets(int[] nums) {
    List<List<Integer>> result = new ArrayList<>();
    dfs(nums, 0, new ArrayList<>(), result);
    return result;
  }

  private void dfs(int[] nums, int start, List<Integer> list, List<List<Integer>> result) {
    result.add(new ArrayList<>(list));
    for(int i = start; i < nums.length; i++) {
      list.add(nums[i]);
      dfs(nums, i + 1, list, result);
      list.remove(list.size() - 1);
    }
  }
}
```



## [51. N 皇后](https://leetcode.cn/problems/n-queens/)
```java
class Solution {
  public List<List<String>> solveNQueens(int n) {
    List<List<String>> result = new ArrayList<>();
    char[][] board = new char[n][n];
    for (int i = 0; i < n; i++) {
      for (int j = 0; j < n; j++) {
        board[i][j] = '.';
      }
    }

    dfs(n, 0, board, result);
    return result;
  }

  private void dfs(int n, int row, char[][] board, List<List<String>> result) {

    if (row == n) {
      List<String> list = new ArrayList<>();
      for (char[] chars : board) {
        list.add(String.valueOf(chars));
      }
      result.add(list);
      return;
    }
    for (int col = 0; col < n; col++) {
      if (isValid(board, row, col)) {
        board[row][col] = 'Q';
        dfs(n, row + 1, board, result);
        board[row][col] = '.';
      }
    }
  }

  private boolean isValid(char[][] board, int row, int col) {
    // 判断对应的列是否有冲突的棋子
    for (int i = 0; i < row; i++) {
      if (board[i][col] == 'Q') {
        return false;
      }
    }
    // 判断上对角线是否有冲突的棋子
    for (int i = 1; i <= Math.min(row, col); i++) {
      if (board[row - i][col - i] == 'Q') {
        return false;
      }
    }

    // 判断下对角线是否有冲突的棋子
    int n = board.length;
    for (int i = 1; i <= Math.min(row, n - 1 - col); i++) {
      if (board[row - i][col + i] == 'Q') {
        return false;
      }
    }
    return true;
  }
}
```

## [46. 全排列](https://leetcode.cn/problems/permutations/)
```java
class Solution {
  public List<List<Integer>> permute(int[] nums) {
    List<List<Integer>> result = new ArrayList<>();
    dfs(nums, 0, new ArrayList<>(), result, new boolean[nums.length]);
    return result;
  }

  private void dfs(int[] nums, int depth, List<Integer> list, List<List<Integer>> result, boolean[] visited) {
    if(depth == nums.length) {
      // 浅拷贝
      result.add(new ArrayList<>(list));
      return;
    }
    for(int i = 0; i < nums.length; i++) {
      if(!visited[i]) {
        list.add(nums[i]);
        visited[i] = true;
        dfs(nums, depth + 1, list, result, visited);
        visited[i] = false;
        list.remove(list.size() - 1);
      }
    }
  }
}
```

时间复杂度O(N^N)，空间复杂度为O(N)

## [17. 电话号码的字母组合](https://leetcode.cn/problems/letter-combinations-of-a-phone-number/)
```java
class Solution {
  public List<String> letterCombinations(String digits) {
    if(digits.length() == 0) {
      return new ArrayList<>();
    }
    Map<Character, String> phoneMap = new HashMap<>() {{
      put('2', "abc");
      put('3', "def");
      put('4', "ghi");
      put('5', "jkl");
      put('6', "mno");
      put('7', "pqrs");
      put('8', "tuv");
      put('9', "wxyz");
    }};

    List<String> result = new ArrayList<>();
    dfs(digits, 0, new StringBuilder(), result, phoneMap);
    return result;
  }
  private void dfs(String digits, int start, StringBuilder sb, List<String> result, Map<Character, String> phoneMap) {
    if(start == digits.length()) {
      result.add(sb.toString());
      return;
    }
    String str = phoneMap.get(digits.charAt(start));
    if(str == null) {
      return;
    }
    for(int i = 0; i < str.length(); i++) {
      sb.append(str.charAt(i));
      dfs(digits, start + 1, sb, result, phoneMap);
      sb.deleteCharAt(sb.length() - 1);
    }
  }
}
```



## [784. 字母大小写全排列](https://leetcode.cn/problems/letter-case-permutation/)
```java
class Solution {
  public List<String> letterCasePermutation(String s) {

    List<String> result = new ArrayList<>();
    dfs(s, 0, new StringBuilder(), result);
    return result;
  }

  private void dfs(String s, int start, StringBuilder sb, List<String> result) {
    if (start == s.length()) {
      result.add(sb.toString());
      return;
    }

    if (Character.isLetter(s.charAt(start))) {
      // 大写
      sb.append(Character.toUpperCase(s.charAt(start)));
      dfs(s, start + 1, sb, result);
      sb.deleteCharAt(sb.length() - 1);
      // 小写
      sb.append(Character.toLowerCase(s.charAt(start)));
      dfs(s, start + 1, sb, result);
      sb.deleteCharAt(sb.length() - 1);
    } else {
      sb.append(s.charAt(start));
      dfs(s, start + 1, sb, result);
      sb.deleteCharAt(sb.length() - 1);
    }
  }
}
```



## [47. 全排列 II](https://leetcode.cn/problems/permutations-ii/)
```java
class Solution {
  public List<List<Integer>> permuteUnique(int[] nums) {
    Arrays.sort(nums);
    List<List<Integer>> result = new ArrayList<>();
    dfs(nums, 0, new ArrayList<>(), result, new boolean[nums.length]);
    return result;

  }
  private void dfs(int[] nums, int start, List<Integer> list, List<List<Integer>> result, boolean[] visited) {
    if(start == nums.length) {
      result.add(new ArrayList<>(list));
      return ;
    }
    for(int i = 0; i < nums.length; i++) {
      // 由于nums是有序的，如果nums[i] == nums[i-1]的时候，visited[i - 1]为true时说明不是第一层递归，可以继续；
      // 如果是第一层递归的话，由于前面已经遍历过得出一个全排列了，再递归一次就会得到重复的结果
      if(!visited[i] && (i == 0 || nums[i] != nums[i - 1] || visited[i - 1])) {
        list.add(nums[i]);
        visited[i] = true;
        dfs(nums, start + 1, list, result, visited);
        visited[i] = false;
        list.remove(list.size() - 1);
      }
    }
  }
}
```

## [39. 组合总和](https://leetcode.cn/problems/combination-sum/)
```java
class Solution {
  public List<List<Integer>> combinationSum(int[] candidates, int target) {
    List<List<Integer>> result = new ArrayList<>();
    dfs(candidates, 0, 0, target, new ArrayList<>(), result);
    return result;
  }
  private void dfs(int[] candidates, int index, int sum, int target, List<Integer> list, List<List<Integer>> result) {
    if(sum == target) {
      result.add(new ArrayList<>(list));
      return ;
    }
    if(sum > target) {
      return ;
    }
    // 只需要不断的向后遍历，向后遍历会考虑到后面的数的所有情况，比如
    //[1, 3, 2], target = 6。组合b包含1的可能有[1,1,1,1,1,1],[1,1,2,2],[1,1,1,1,2],[1,2,3]
    // 事实上，1的情况遍历完后，以2、3开始遍历的时候，就不需要考虑还有1的组合了，如果以3开始的话，组合[3, 3]
    // 以2开始遍历的话，组合[2,2,2];
    for(int i = index; i < candidates.length; i++) {
      list.add(candidates[i]);
      dfs(candidates, i, sum + candidates[i], target, list, result);
      list.remove(list.size() - 1);
    }
  }
}
```



## [77. 组合](https://leetcode.cn/problems/combinations/)
```java
class Solution {
  public List<List<Integer>> combine(int n, int k) {
    List<List<Integer>> result = new ArrayList<>();
    dfs(n, k, 1, new ArrayList<>(), result);
    return result;
  }
  private void dfs(int n, int k, int start, List<Integer> list, List<List<Integer>> result) {
    if(list.size() == k) {
      result.add(new ArrayList<>(list));
      return ;
    }
    for(int i = start; i <= n; i++) {
      list.add(i);
      dfs(n, k, i + 1, list, result);
      list.remove(list.size() - 1);
    }
  }
}
```

## [40. 组合总和 II](https://leetcode.cn/problems/combination-sum-ii/)
跟47题很类似

```java
class Solution {
  public List<List<Integer>> combinationSum2(int[] candidates, int target) {
    Arrays.sort(candidates);
    List<List<Integer>> result = new ArrayList<>();
    dfs(candidates, target, 0, 0, new ArrayList<>(), result, new boolean[candidates.length]);
    return result;
  }
  // [1, 2, 2, 2, 5] 5
  private void dfs(int[] candidates, int target, int start, int sum, List<Integer> list, List<List<Integer>> result, boolean[] visited) {
    if(sum > target) {
      return;
    }
    if(sum == target) {
      result.add(new ArrayList<>(list));
      return;
    }

    for(int i = start; i < candidates.length; i++) {
      // candidates = [1,1,1,1,3] target = 5
      // 连续相同的数字只能挨着选
      if(i == 0 || candidates[i] != candidates[i - 1] || visited[i - 1]) {
        visited[i] = true;
        list.add(candidates[i]);
        dfs(candidates, target, i + 1, sum + candidates[i], list, result, visited);
        list.remove(list.size() - 1);
        visited[i] = false;
      }

    }
  }
}
```

## [1593. 拆分字符串使唯一子字符串的数目最大](https://leetcode.cn/problems/split-a-string-into-the-max-number-of-unique-substrings/)
```java
class Solution {
  private int result = 0;
  public int maxUniqueSplit(String s) {

    dfs(s, 0, new HashSet<>());
    return result;

  }
  private void dfs(String s, int start, Set<String> set) {
    if(start == s.length()) {
      result = Math.max(result, set.size());
      return ;
    }

    for(int i = start; i < s.length(); i++) {
      String sub = s.substring(start, i + 1);
      if(!set.contains(sub)) {
        set.add(sub);
        dfs(s, i + 1, set);
        set.remove(sub);
      }
    }
  }
}
```

时间复杂度：O(2^N)，字符串长度为N，每个字符可作为拆分点或不作为拆分点。



优化点：每次需要组合完才判断大小，有可能最长的答案已经出现过，所以后面的判断都比较多余。

于是可以在减枝上优化。

```java
class Solution {
  private int result = 0;
  public int maxUniqueSplit(String s) {

    dfs(s, 0, new HashSet<>());
    return result;

  }
  private void dfs(String s, int start, Set<String> set) {
    if(set.size() + s.length() - start <= result) {
      return ;
    }
    if(start == s.length()) {
      result = Math.max(result, set.size());
      return ;
    }

    for(int i = start; i < s.length(); i++) {
      String sub = s.substring(start, i + 1);
      if(!set.contains(sub)) {
        set.add(sub);
        dfs(s, i + 1, set);
        set.remove(sub);
      }
    }
  }
}
```

## [1079. 活字印刷](https://leetcode.cn/problems/letter-tile-possibilities/)
### 穷举组合
```java
class Solution {
  public int numTilePossibilities(String tiles) {

    char[] chars = tiles.toCharArray();
    Arrays.sort(chars);
    int len = chars.length;
    boolean[] visited = new boolean[len];
    Set<String> res = new HashSet<>();
    List<Character> ans = new LinkedList<>();
    dfs(chars, 0, ans, res, visited, len);
    int size = res.size();
    return size == 0 ? 0 : size - 1;
  }
  private void dfs(char[] tiles, int depth, List<Character> ans, Set<String> res, boolean[] visited, int len) {
    if(depth == len) {
      String str = ans.toString();
      if(!res.contains(str)) {
        res.add(str);
      }
      return;
    }
    for(int i = 0; i < len; i++) {
      if(!visited[i] && (i == 0 || tiles[i] != tiles[i-1] || visited[i-1])) {
        // 不选
        visited[i] = true;
        dfs(tiles, depth + 1, ans, res, visited, len);
        visited[i] = false;
        // 选
        ans.add(tiles[i]);
        visited[i] = true;
        dfs(tiles, depth + 1, ans, res, visited, len);
        visited[i] = false;
        ans.remove(ans.size() - 1);
      }
    } 

  }
}
```



<font style="color:rgb(78, 83, 88);">虽然做出来了，但是时间复杂度和空间复杂度太高。</font>

<font style="color:rgb(78, 83, 88);">仔细想想这个过程，其实我是想通过组合的方式穷举每一种可能性，但在这个过程中会产生大量重复的数据，</font>

<font style="color:rgb(78, 83, 88);">比如【不选A1、选A2、不选B】 就等同于 【不选A1、不选B、选A2】、【选A1、不选A2、不选B】，就非常冗余。最后再进行去重，这样的时间复杂度就变成了</font>`<font style="color:rgb(78, 83, 88);">2*n * (2*(n-1)) * (2*(n-2)) * ... = 2^n * n!</font>`<font style="color:rgb(78, 83, 88);"> 。</font>

<!-- 这是一张图片，ocr 内容为：ROOT 以第一位开始 以第三位开始 选AL:"A" 不选A! 选B:AB 选A2:AA 不选A2:A" 不选B:* 选B:"8* 话A2:A" 不选A2: 不选B:A 送8:8 不选B:A" 不选B:AAB :AAB" 选B:AB 选A2:A 不选A2: 选8:* 不选8 选A2:AA 不选A2:A 不选8: 不选A2:"AB"选A2:ABA" 选A2:BA" 不选A2: -->
![](https://cdn.nlark.com/yuque/0/2023/png/32473878/1680319294711-9c301c49-1c6e-4471-a164-efb6e9606b36.png)

<font style="color:rgb(78, 83, 88);">上面我们是在遍历完之后再去重算结果，就相当于是在统计叶子节点，中间的节点产生大量的重复。</font>

### <font style="color:rgb(78, 83, 88);">取巧</font>
<font style="color:rgb(78, 83, 88);">如果我们中间的结点也是有意义的呢？比如下面这个遍历，过程就是最后的结果。</font>

<font style="color:rgb(78, 83, 88);">最后的答案是有关联的，比如AAB的答案其实是在AA上得到的，ABA是在AB上得到的，三位数的答案可以从二位数的答案推断出来，二位数的答案可以从一位数推断出来。所以只需要遍历一遍即可，时间复杂度为O(n)。</font>

<!-- 这是一张图片，ocr 内容为：ROOT 以第三位开始 以第一位开始 选B:"B" 选A1:"A" 选A1:"BA" 选B:"AB" 选A2:"AA" 选B:"AAB" 8:选A2:"BAA" 选A2:"ABA" -->
![](https://cdn.nlark.com/yuque/0/2023/png/32473878/1680319304332-98faaa87-6f62-4a80-ae9f-78fd4dc1953a.png)

```java
class Solution {
  int count = 0;
  public int numTilePossibilities(String tiles) {

    char[] chars = tiles.toCharArray();
    Arrays.sort(chars);
    boolean[] visited = new boolean[chars.length];
    dfs(chars, 0, visited);
    return count;
  }
  private void dfs(char[] tiles, int depth, boolean[] visited) {
    if(depth == tiles.length) {
      return;
    }
    for(int i = 0; i < tiles.length; i++) {
      if(!visited[i] && (i == 0 || tiles[i] != tiles[i-1] || visited[i-1])) {
        count++;
        visited[i] = true;
        dfs(tiles, depth + 1, visited);
        visited[i] = false;      
      }
    } 
  }
}
```

时间复杂度为O(2^N)



### <font style="color:rgb(78, 83, 88);">频数统计</font>
  
<!-- 这是一张图片，ocr 内容为："活字印刷"题解配图(13) LEETCODE第1079题: 递归回溯按照右下角的编号顺序执行. 说明:A:2,B:1表示"A"还有2个可以用,"B"还有1个可以用. 递归从根结点开始. A:2,B:1 A:2,B:0 A:1,B:1 AA BB AB BA A:0,B:1 A:1,B:0 A:1,B:0 ABB ABA BAA BAB AAA AAB 8 A:0,B:0 A:0,B:0 A:0,B:0 红色表示有字母不够用了, 发生剪枝. -->
![](https://cdn.nlark.com/yuque/0/2023/png/32473878/1680319335953-095d3d30-1b30-485d-a151-aaa096d6a2e6.png)

```java
class Solution {
  int count = 0;
  public int numTilePossibilities(String tiles) {

    char[] chars = tiles.toCharArray();
    int[] cnt = new int[26];
    for(int i = 0; i < chars.length; i++) {
      cnt[chars[i] - 'A']++;
    }
    dfs(0, cnt);
    return count;
  }
  private void dfs(int depth, int[] cnt) {
    for(int i = 0; i < cnt.length; i++) {
      if(cnt[i] > 0) {
        count++;
        cnt[i]--;
        dfs(depth + 1, cnt);
        cnt[i]++;
      }
    } 
  }
}
```



当然，如果想设计递归返回的答案，也可以这样做，dfs(cnt)返回的是当前频数下的组合值

```java
public class Solution {

  public int numTilePossibilities(String tiles) {
    int[] count = new int[26];
    char[] charArray = tiles.toCharArray();
    for (char c : charArray) {
      count[c - 'A']++;
    }
    // tiles 里所有的信息都存在 count 里，对 count 执行深度优先遍历即可
    return dfs(count);
  }

  /**
  * 设计递归函数的返回值
  *
  * @return 在当前的频数数组下，可以产生的排列数
  */
  private int dfs(int[] count) {
    // 递归终止条件是：当前没有可以用的字符（没有显式递归终止条件）
    int res = 0;
    for (int i = 0; i < 26; i++) {
      if (count[i] != 0) {
        res++;
        count[i]--;
        res += dfs(count);
        count[i]++;
      }
      
    }
    return res;
  }
}
```



## [79. 单词搜索](https://leetcode.cn/problems/word-search/)


```java
class Solution {
  private int[][] directions = { { 1, 0 }, { -1, 0 }, { 0, 1 }, { 0, -1 } };

  public boolean exist(char[][] board, String word) {
    int m = board.length;
    int n = board[0].length;
    boolean[][] visited = new boolean[m][n];
    for (int i = 0; i < m; i++) {
      for (int j = 0; j < n; j++) {
        if (word.charAt(0) == board[i][j]) {
          visited[i][j] = true;
          // 起点已经匹配了 word[0]，所以传 0
          if (dfs(board, 0, i, j, word, visited)) {
            return true;
          }
          visited[i][j] = false;
        }

      }
    }
    return false;
  }

  // 约定：进入时 board[x][y] == word[index]，且 word[0..index] 都已匹配
  private boolean dfs(char[][] board, int index, int x, int y, String word, boolean[][] visited) {
    // 当前格子已经是最后一个字符，整个 word 匹配完成
    if (index == word.length() - 1) {
      return true;
    }
    int m = board.length;
    int n = board[0].length;
    for (int[] direction : directions) {
      int newX = x + direction[0];
      int newY = y + direction[1];
      // 先验证邻居是不是下一个字符，验证通过才递归进去
      if (!OffGrid(newX, newY, m, n) && !visited[newX][newY]
          && board[newX][newY] == word.charAt(index + 1)) {
        visited[newX][newY] = true;
        if (dfs(board, index + 1, newX, newY, word, visited)) {
          return true;
        }
        visited[newX][newY] = false;
      }
    }
    return false;
  }

  private boolean OffGrid(int x, int y, int m, int n) {
    return x < 0 || x >= m || y < 0 || y >= n;
  }
}
```



## 总结
自顶向下，是从头算到尾，然后再把答案层层传递到头上，中间的结点拿到的结果也就是从头到尾的结果。

自底向上 其实就是不断的 求解子问题，可以记忆化搜索

